[package]
name = "fleet-normalizer"
version = "0.1.0"
edition = "2024"
license = "AGPL-3.0-or-later"
description = "Rust fleet telemetry normalizer for safety event streams."

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
